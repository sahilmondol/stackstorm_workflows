version: 1.0

description: A basic workflow that prints a user's input.

input:
  - message

tasks:
  task1:
    action: stackstorm_workflows.orquesta-basic message=<% message %>

output:
  - stdout: <% message %>